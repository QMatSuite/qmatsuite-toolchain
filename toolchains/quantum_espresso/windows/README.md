# QE Windows Toolchains

This directory hosts parallel Windows recipes:

- `mingw/` – MSYS2 MinGW-w64 toolchain (current CI). See `mingw/README.md` for details and patches.
- `oneapi/` – Intel oneAPI/ifx recipe (local build instructions live here).

