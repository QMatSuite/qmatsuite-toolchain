# Wannier90 Toolchain (planned)

This directory is reserved for future Wannier90 build scripts and CI.

Planned:

- Build Wannier90 on Linux and macOS (and optionally Windows)
- Integrate with Quantum ESPRESSO builds (e.g. `pw2wannier90.x`)

For now, only Quantum ESPRESSO is built in CI.

