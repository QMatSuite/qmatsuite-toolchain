# Wannier90 Windows Toolchains

Windows recipes are organized by toolchain:

- `mingw/` – MSYS2 MinGW-w64 build (current CI). See `mingw/README.md` and supporting scripts/patches inside.
- `oneapi/` – Placeholder for an Intel oneAPI-based build recipe.

